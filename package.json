{
  "name": "@is4co/rootstock",
  "version": "0.1.0",
  "description": "One interface over code-editing agents: a driver contract, a normalized event stream, and a conformance suite.",
  "type": "module",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "oxlint --deny-warnings src && tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "oxlint": "^1.79.0",
    "typescript": "^7.0.2"
  }
}
